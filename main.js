const myModule = require("./module.js");
        /* 모듈js을 모듈 폴더에 넣고 index.js로 바꿈
        const myModule = require("./module");
        그러면 폴더명만 써서 사용함. 이 방법이 많이 사용됨
        /*

console.log(myModule);
console.log(myModule.a);
myModule.b();